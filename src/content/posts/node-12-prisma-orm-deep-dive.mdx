---
title: 'Node.js Super Senior · Phase 12 — Prisma ORM Deep Dive'
description: 'Bonus Phase 12: master Prisma — the schema and relations, migrations, the type-safe Client (CRUD, select/include, nested writes, aggregations), transactions, connection pooling, safe raw SQL, extensions, and error handling.'
pubDate: 2026-06-09
tags: [nodejs, prisma, orm, postgresql, node_10]
series: nodejs
seriesOrder: 12
draft: false
---

Đây là **Bonus Phase 12**. Phase 11 cho bạn làm chủ PostgreSQL thô — nền tảng. Giờ ta thêm **Prisma**, ORM mà đa số team TypeScript hiện đại chọn. Điểm bán hàng của nó là **type safety đầu-cuối**: schema database sinh ra một client có type đầy đủ, nên field hay type sai là *lỗi compile*, không phải sự cố production lúc 2 giờ sáng.

Mọi thứ bạn học ở Phase 11 vẫn áp dụng — Prisma sinh SQL; bạn vẫn phải hiểu index, transaction, và N+1.

---

## Prisma là gì

Prisma có ba phần: Cộng **Studio**, một GUI cho dữ liệu.

```bash
npm install prisma --save-dev
npm install @prisma/client
npx prisma init --datasource-provider postgresql   # creates prisma/schema.prisma + .env
```

```text
schema.prisma  ──prisma migrate──▶  database (SQL DDL)
      │
      └──prisma generate──▶  @prisma/client  (typed query API)
```

---

## Schema — nguồn sự thật duy nhất

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String?
  role      Role     @default(USER)
  posts     Post[]                            // 1-to-many: a user has many posts
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")                              // actual table name
}

model Post {
  id        String   @id @default(uuid())
  title     String
  body      String
  published Boolean  @default(false)
  author    User     @relation(fields: [authorId], references: [id], onDelete: Cascade)
  authorId  String
  tags      Tag[]                             // implicit many-to-many

  @@index([authorId, published])              // composite index (Phase 11 rules apply)
  @@map("posts")
}

model Tag {
  id    String @id @default(uuid())
  name  String @unique
  posts Post[]
}

enum Role { USER ADMIN }
```

Đọc cái này như DDL ở Phase 11: `@id`/`@default`/`@unique` `@id`/`@default`/`@unique` là constraint cột, `@relation` định nghĩa khóa ngoại kèm hành động tham chiếu, `@@index` là index ghép, `@@map` tách tên model khỏi tên bảng.

### Quan hệ

- FK vô hướng một bên + danh sách bên kia.
- *ngầm* hoặc *tường minh* khi bảng nối có field thêm.
- một FK `@unique`.

---

## Migration

```bash
npx prisma migrate dev --name add_posts   # dev: creates + applies a migration, regenerates client
npx prisma migrate deploy                  # prod/CI: applies pending migrations only
npx prisma migrate reset                   # wipe + replay (dev only)
npx prisma db push                         # prototype: sync schema WITHOUT a migration file
```

Phân biệt senior: `migrate dev` tạo migration SQL có version, commit (dùng mọi nơi thật); `db push` cho prototype nhanh và bỏ lịch sử. Trong CI/CD, chạy `migrate deploy` như **bước riêng, tương thích ngược, trước** khi code mới lên.

---

## Client — CRUD có type

```ts
import { PrismaClient, Prisma } from '@prisma/client';
export const prisma = new PrismaClient();

// CREATE — input type is generated; a typo or wrong type won't compile
const user = await prisma.user.create({ data: { email: 'a@b.com', name: 'Ann' } });

// READ — findUnique by a unique field; select trims the payload (and its type!)
const u = await prisma.user.findUnique({
  where: { email: 'a@b.com' },
  select: { id: true, email: true },   // returned type is { id: string; email: string }
});

// READ many — filter, sort, paginate
const posts = await prisma.post.findMany({
  where: { published: true, author: { role: 'ADMIN' } },  // filter across the relation
  orderBy: { createdAt: 'desc' },
  take: 20,
  skip: 0,
});

// UPDATE / DELETE
await prisma.post.update({ where: { id }, data: { published: true } });
await prisma.post.delete({ where: { id } });
```

### include vs select — và thoát N+1

`include` kéo quan hệ; `select` chọn field chính xác. **Prisma gộp việc tải quan hệ thành vài query**, nên tải post *kèm* author **không** bắn một query mỗi post:

```ts
// ✅ One query for posts + one batched query for all authors — not N+1
const feed = await prisma.post.findMany({
  include: { author: { select: { id: true, name: true } }, tags: true },
});
```

Nhưng bạn vẫn tạo N+1 nếu **await quan hệ trong vòng lặp** — đừng làm vậy.

### Ghi lồng, upsert, tổng hợp

```ts
// Nested write — create a user AND their first post in one atomic call
await prisma.user.create({
  data: { email: 'c@d.com', posts: { create: [{ title: 'Hello', body: '...' }] } },
});

// Upsert — insert or update (maps to ON CONFLICT, Phase 11)
await prisma.tag.upsert({ where: { name: 'node' }, create: { name: 'node' }, update: {} });

// Aggregation + groupBy
const stats = await prisma.post.groupBy({
  by: ['authorId'],
  _count: { _all: true },
  having: { authorId: { _count: { gt: 5 } } },
});
```

---

## Type safety nâng cao

Prisma suy ra type chính xác từ query, nên bạn không tự viết DTO lệch khỏi schema:

```ts
// The exact return type of a specific include/select — reusable across layers
type PostWithAuthor = Prisma.PostGetPayload<{ include: { author: true } }>;

// Validate-and-type an input object without executing it
const data = Prisma.validator<Prisma.UserCreateInput>()({ email: 'x@y.com' });
```

---

## Transaction

Hai kiểu. Dạng **mảng** chạy các thao tác độc lập nguyên tử; dạng **tương tác** cho bạn một client `tx` cho logic đọc-sửa-ghi, có điều khiển cô lập:

```ts
// Array: all-or-nothing, no logic between
await prisma.$transaction([
  prisma.account.update({ where: { id: from }, data: { balance: { decrement: amount } } }),
  prisma.account.update({ where: { id: to },   data: { balance: { increment: amount } } }),
]);

// Interactive: logic + the isolation levels from Phase 11
await prisma.$transaction(async (tx) => {
  const acct = await tx.account.findUniqueOrThrow({ where: { id: from } });
  if (acct.balance < amount) throw new Error('Insufficient funds'); // throwing rolls back
  await tx.account.update({ where: { id: from }, data: { balance: { decrement: amount } } });
  await tx.account.update({ where: { id: to },   data: { balance: { increment: amount } } });
}, { isolationLevel: Prisma.TransactionIsolationLevel.Serializable });
```

> Mọi thứ ở Phase 11 vẫn đúng: dưới `Serializable`, sẵn sàng **retry** khi serialization failure.

---

## Connection pooling & SQL thô

`PrismaClient` pool kết nối; chỉnh `connection_limit` trong `DATABASE_URL`, và tôn trọng công thức Phase 4. Trong serverless, đặt **PgBouncer** phía trước hoặc dùng **Prisma Accelerate**; với runtime khác dùng **driver adapter**.

Khi API Prisma không diễn đạt được, hạ xuống **SQL thô parameterized** — dạng tagged-template an toàn injection:

```ts
// ✅ Safe: ${userInput} is sent as a parameter, never interpolated into SQL
const rows = await prisma.$queryRaw<{ id: string }[]>`
  SELECT id FROM posts WHERE title ILIKE ${'%' + term + '%'} LIMIT 20
`;
await prisma.$executeRaw`UPDATE posts SET views = views + 1 WHERE id = ${id}`;
```

> Đừng dùng `$queryRawUnsafe` với input nối chuỗi — đó là cái bẫy đưa SQL injection trở lại.

---

## Mở rộng client & xử lý lỗi

`$extends` cho bạn thêm hành vi tái dùng — field tính toán, log query, hay **soft delete** — mà không bọc mọi lời gọi:

```ts
const xprisma = prisma.$extends({
  query: {
    post: {
      // Turn every delete into an update (soft delete)
      async delete({ args, query }) {
        return query({ ...args, data: { deletedAt: new Date() } } as never);
      },
    },
  },
});
```

Xử lý lỗi có type của Prisma theo **mã**, không theo message:

```ts
import { Prisma } from '@prisma/client';

try {
  await prisma.user.create({ data });
} catch (err) {
  if (err instanceof Prisma.PrismaClientKnownRequestError) {
    if (err.code === 'P2002') throw new AppError('Email already exists', 409); // unique violation
    if (err.code === 'P2025') throw new NotFoundError();                       // record not found
  }
  throw err;
}
```

> Quy tắc senior: tạo **một** `PrismaClient` cho cả app; ngắt khi tắt.

---

## Prisma vs SQL thô vs ORM khác

| | Raw SQL / `pg` | Prisma | TypeORM / Sequelize |
|---|---|---|---|
| An toàn type | manual | **generated, end-to-end** | partial |
| Migration | hand-written | declarative + generated | decorator/CLI |
| Kiểm soát | total | high (+ raw escape hatch) | high |
| Đường học | SQL | low | medium |

Quan điểm senior: DX và type safety của Prisma khiến nó là mặc định tốt; giữ SQL thô cho query nó không diễn đạt được.

---

## Thực hành

1. mô hình blog trong `schema.prisma` với quan hệ, enum, index ghép; chạy `migrate dev`.
2. dựng repository có type: CRUD + `findMany` lọc/phân trang + ghi lồng; chứng minh type trả được suy ra.
3. cài transaction chuyển tiền tương tác ở `Serializable` kèm retry.
4. thêm extension soft-delete và ánh xạ `P2002`/`P2025` sang `AppError`.

---

## Phần tiếp theo

Giờ bạn dùng Prisma như senior: schema có type và quan hệ, migration có version, API query đầy đủ không N+1, type suy ra chính xác, transaction có cô lập, pooling hợp lý, SQL thô an toàn, extension, và xử lý lỗi theo mã.

Nút cổ chai kế tiếp trong mọi backend nghiêm túc là làm *cùng việc tốn kém* lặp đi lặp lại — nên ở **Phase 13** ta thêm cache và hàng đợi job với **Redis**: pattern cache và TTL, pub/sub và stream, rate limit, khóa phân tán, session, và job nền BullMQ.
