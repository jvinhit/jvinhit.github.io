---
title: 'TypeScript Production · Phần 5 — Runtime Boundaries & Domain Types'
description: 'Biến unknown từ API, storage và env thành domain value đáng tin: parse at the edge, schema evolution, branded types, DTO mapping và contract versioning.'
pubDate: 2026-06-27
lang: vi
tags: [typescript, validation, domain-modeling, api]
series: typescript
seriesOrder: 5
draft: false
---

Type annotation không validate JSON. Senior TypeScript đặt một quy tắc cứng: **dữ liệu ngoài process là `unknown` cho tới khi được parse**.

## Boundary inventory

- HTTP request/response, webhook, message queue;
- `localStorage`, IndexedDB, cookie;
- environment variable và config file;
- form, URL/search params, DOM dataset;
- dependency không typed hoặc typed quá lạc quan.

```ts
async function getJson(url: string): Promise<unknown> {
  const response = await fetch(url);
  if (!response.ok) throw new Error(`HTTP ${response.status}`);
  return response.json();
}
```

Đừng viết `response.json() as User`. Assertion đó biến network thành nguồn “đáng tin” mà không tạo bằng chứng.

## Parse, rồi map sang domain

Ví dụ dùng một interface parser tối giản để không khóa kiến trúc vào thư viện cụ thể:

```ts
type Parser<T> = { parse(input: unknown): T };

type UserDto = {
  id: string;
  display_name: string;
  created_at: string;
};

type User = {
  id: UserId;
  displayName: string;
  createdAt: Date;
};

async function loadUser(id: UserId, parser: Parser<UserDto>): Promise<User> {
  const dto = parser.parse(await getJson(`/api/users/${id}`));
  return {
    id: parseUserId(dto.id),
    displayName: dto.display_name,
    createdAt: parseIsoDate(dto.created_at),
  };
}
```

DTO phản ánh wire contract. Domain type phản ánh điều application cần. Mapping này hấp thụ snake_case, date string, nullable legacy field và version drift ở một chỗ.

## Parse, don't validate-and-forget

```ts
function isPositive(value: number): boolean {
  return Number.isFinite(value) && value > 0;
}
```

Boolean validator bắt caller nhớ correlation. Parser trả về type đã tinh chỉnh:

```ts
declare const positiveBrand: unique symbol;
type Positive = number & { readonly [positiveBrand]: true };

function parsePositive(value: unknown): Positive {
  if (typeof value !== 'number' || !Number.isFinite(value) || value <= 0) {
    throw new Error('Expected a positive finite number');
  }
  return value as Positive;
}
```

## Error của boundary là dữ liệu vận hành

Parser error nên cho biết:

- boundary nào (`GET /users/:id`, `APP_CONFIG`);
- path nào (`profile.address.zip`);
- expected/received ở mức an toàn;
- correlation ID hoặc schema version;
- không log secret/PII thô.

Đừng catch validation error rồi trả `null`; bạn sẽ biến contract regression thành bug xa nguồn.

## Evolution: tolerant reader có giới hạn

Optional field hợp lý khi producer thật sự cho phép vắng. Không biến mọi field thành optional để “chống API đổi”. Hãy version rõ khi meaning đổi.

```ts
type UserEvent =
  | { version: 1; type: 'user.created'; name: string }
  | { version: 2; type: 'user.created'; firstName: string; lastName: string };

function normalize(event: UserEvent): CurrentUserCreated {
  switch (event.version) {
    case 1:
      return migrateV1(event);
    case 2:
      return event;
    default:
      return assertNever(event);
  }
}
```

## Boundary architecture

```text
unknown bytes
  → transport checks (status/content-type/size)
  → schema parse
  → DTO
  → domain mapping + invariants
  → trusted core
```

Core không nên import schema library nếu chỉ boundary cần nó. Điều này giữ domain test nhanh và tránh vendor type lan khắp codebase.

## Lab

1. Liệt kê mọi `as SomeResponse`; thay một flow bằng `unknown → parse → map`.
2. Thêm malformed payload tests: thiếu field, sai type, date vô nghĩa, field thừa.
3. Thiết kế version migration cho một object lưu ở localStorage.
4. Đo log khi schema fail và loại bỏ PII.

**Done khi:** không có dữ liệu ngoại vi nào vào domain chỉ nhờ annotation/assertion; contract regression fail ngay tại boundary.

## Đọc tiếp

- [TypeScript type declarations](https://www.typescriptlang.org/docs/handbook/2/type-declarations.html)
- [Narrowing `unknown`](https://www.typescriptlang.org/docs/handbook/2/narrowing.html)
