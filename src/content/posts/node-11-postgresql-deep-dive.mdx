---
title: 'Node.js Super Senior · Phase 11 — PostgreSQL Deep Dive'
description: 'Bonus Phase 11: go deep on PostgreSQL from Node — data modeling, the right index per query, EXPLAIN ANALYZE, transactions and isolation levels, JSONB, connection pooling with pg, and the concurrency bugs that bite seniors.'
pubDate: 2026-06-09
tags: [nodejs, postgresql, database, sql, node_10]
series: nodejs
seriesOrder: 11
draft: false
---

Đây là **Bonus Phase 11**, phần đầu trong ba deep dive nối tiếp mười phase cốt lõi — PostgreSQL, Prisma, rồi NestJS. Phase 4 cho bạn bề rộng về database SQL và NoSQL. Giờ ta đi sâu vào database mà đa số backend Node senior thực sự chạy: **PostgreSQL**. Junior viết query trả đúng dòng; senior viết query vẫn đúng khi đồng thời và nhanh khi bảng lớn tới hàng triệu dòng.

---

## Vì sao PostgreSQL

Postgres là lựa chọn senior mặc định cho dữ liệu quan hệ: mã nguồn mở, tuân chuẩn, và gói đủ tính năng như một danh sách ước — transaction ACID thật, kiểu phong phú, đánh index mạnh, full-text search, window function, và `LISTEN/NOTIFY` cho pub/sub.

```bash
docker run --name pg -e POSTGRES_PASSWORD=secret -p 5432:5432 -d postgres:17
```

> Ghi chú senior: mặc định chọn Postgres; chỉ chọn cái khác khi bạn nêu được đúng tính chất nó thiếu.

---

## Mô hình hóa dữ liệu & chuẩn hóa

Thiết kế schema tốt là hiệu năng rẻ nhất bạn từng mua. Chuẩn hóa về **3NF** mặc định — mọi cột non-key phụ thuộc vào key, toàn bộ key, và chỉ key.

```sql
CREATE TABLE users (
  id          bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  email       citext UNIQUE NOT NULL,          -- case-insensitive text
  created_at  timestamptz NOT NULL DEFAULT now()
);

CREATE TABLE posts (
  id          bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
  author_id   bigint NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  title       text NOT NULL,
  body        text NOT NULL,
  status      text NOT NULL DEFAULT 'draft'
              CHECK (status IN ('draft','published','archived')),
  published_at timestamptz,
  created_at  timestamptz NOT NULL DEFAULT now()
);
```

Thói quen senior trong DDL đó: `bigint` khóa `bigint`, `timestamptz` không phải `timestamp`, `citext` cho email, `CHECK` thay vì tin app, và `ON DELETE CASCADE` để ý định tham chiếu rõ ràng.

> Phi chuẩn hóa **có chủ ý và về sau** — chỉ khi một đường đọc đã đo đòi hỏi, không bao giờ là điểm bắt đầu.

---

## Index — đúng loại cho mỗi query

Index là một đánh đổi: đọc nhanh hơn đổi lấy ghi chậm hơn và tốn đĩa hơn. Kỹ năng senior là khớp loại index với pattern truy cập.

| Index | Dùng cho |
| --- | --- |
| **B-tree** (default) | equality & range: `=`, `<`, `>`, `BETWEEN`, `ORDER BY` |
| **GIN** | `JSONB`, arrays, full-text (`@>`, `?`, `@@`) |
| **GiST** | geometry, ranges, nearest-neighbour |
| **BRIN** | huge, naturally-ordered tables (time-series) |
| **Hash** | equality only, rarely worth it over B-tree |

```sql
-- Composite index: column order matters — most selective / leading filter first
CREATE INDEX idx_posts_author_status ON posts (author_id, status);

-- Partial index: index only the rows you actually query
CREATE INDEX idx_posts_published ON posts (published_at DESC)
  WHERE status = 'published';

-- Covering index: satisfy a query from the index alone (index-only scan)
CREATE INDEX idx_posts_list ON posts (author_id) INCLUDE (title, created_at);

-- GIN for JSONB containment
CREATE INDEX idx_posts_meta ON posts USING gin (meta jsonb_path_ops);
```

> Ghi chú senior: index ghép `(a, b)` phục vụ query lọc theo `a` hoặc `a,b` — nhưng **không** phục vụ `b` đứng một mình. Sắp cột theo cách bạn lọc.

---

## Đọc plan: EXPLAIN ANALYZE

Đừng đoán vì sao query chậm — hỏi planner:

```sql
EXPLAIN (ANALYZE, BUFFERS)
SELECT id, title FROM posts
WHERE author_id = 42 AND status = 'published'
ORDER BY published_at DESC LIMIT 20;
```

Senior tìm gì:

- `Seq Scan` trên bảng lớn trong query có lọc → thiếu index.
- tốt; index đang được dùng.
- ước lượng vs thực tế lệch xa → thống kê cũ; chạy `ANALYZE`.
- bước `Sort` tràn ra đĩa → cân nhắc index khớp `ORDER BY`.

---

## Transaction & mức cô lập

Transaction là tất-cả-hoặc-không. Nhưng nó ngăn *những* bất thường nào phụ thuộc vào **mức cô lập** — chủ đề đa số dev backend không thật sự học.

| Level | Ngăn | Cho phép |
| --- | --- | --- |
| `READ COMMITTED` (default) | dirty reads | non-repeatable & phantom reads |
| `REPEATABLE READ` | + non-repeatable reads | (in PG) blocks phantoms too |
| `SERIALIZABLE` | everything — as if serial | may abort with a serialization error |

```ts
import { Pool } from 'pg';
const pool = new Pool({ connectionString: process.env.DATABASE_URL });

async function transfer(from: number, to: number, cents: number) {
  const client = await pool.connect();
  try {
    await client.query('BEGIN ISOLATION LEVEL SERIALIZABLE');
    await client.query('UPDATE accounts SET balance = balance - $1 WHERE id = $2', [cents, from]);
    await client.query('UPDATE accounts SET balance = balance + $1 WHERE id = $2', [cents, to]);
    await client.query('COMMIT');
  } catch (err) {
    await client.query('ROLLBACK');
    throw err; // on a 40001 serialization_failure, the caller should retry
  } finally {
    client.release(); // ALWAYS release back to the pool
  }
}
```

> Ghi chú senior: dưới `SERIALIZABLE`, Postgres có thể hủy transaction với mã `40001`. Đó không phải lỗi — là engine bảo bạn **thử lại**. Bọc transaction kiểu này trong vòng retry nhỏ.

### Lỗi mất cập nhật

Đọc-sửa-ghi qua hai request âm thầm mất dữ liệu. Sửa bằng cách khóa dòng hoặc tính ngay trong SQL:

```sql
-- Pessimistic lock: hold the row until COMMIT
SELECT balance FROM accounts WHERE id = $1 FOR UPDATE;

-- Or avoid the round trip entirely — atomic in one statement
UPDATE accounts SET balance = balance - $1 WHERE id = $2 AND balance >= $1;
```

---

## JSONB — quan hệ gặp tài liệu

`JSONB` cho bạn lưu dữ liệu không schema bên trong bảng quan hệ — tốt cho metadata linh hoạt, cài đặt, hay payload sự kiện.

```sql
ALTER TABLE posts ADD COLUMN meta jsonb NOT NULL DEFAULT '{}';

-- containment query, accelerated by the GIN index from 11.2
SELECT id FROM posts WHERE meta @> '{"featured": true}';

-- extract a value; ->> returns text, -> returns jsonb
SELECT meta->>'source' AS source FROM posts WHERE id = $1;
```

> Đừng dùng `JSONB` làm cớ né mô hình hóa. Nếu bạn liên tục query hay join trên một field, nâng nó thành cột thật, có index.

---

## Pool kết nối với `pg`

Mở kết nối Postgres tốn kém (server tạo một process mỗi kết nối). **Luôn** dùng pool, và tham số hóa mọi query để diệt SQL injection:

```ts
import { Pool } from 'pg';

export const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  max: 10,                       // tune to (cores × 2..4), not "bigger is better"
  idleTimeoutMillis: 30_000,
  connectionTimeoutMillis: 5_000,
});

// Parameterized — NEVER string-concatenate user input into SQL
const { rows } = await pool.query(
  'SELECT id, title FROM posts WHERE author_id = $1 LIMIT $2',
  [authorId, 20],
);
```

> Ghi chú senior: trong serverless hay fan-out cao, đặt **PgBouncer** (transaction pooling) trước Postgres — hàng nghìn kết nối app gộp về vài kết nối thật.

---

## MVCC, VACUUM & phình

Khái niệm giải thích hành vi đồng thời của Postgres là **MVCC**: `UPDATE` không ghi đè — nó ghi **phiên bản mới** và đánh dấu cái cũ chết, nên reader không chặn writer và ngược lại.

Hệ quả senior tính đến: phiên bản chết dồn lại thành **bloat**, và `VACUUM` (thường autovacuum) thu hồi.

- bảng thay đổi nhiều cần **autovacuum chỉnh mạnh hơn**.
- transaction chạy lâu giữ snapshot cũ và **chặn vacuum** cả DB.
- `VACUUM` thu hồi để tái dùng; `VACUUM FULL` viết lại bảng và trả đĩa cho OS nhưng khóa độc quyền.

> Giám sát bằng công cụ sẵn có: `pg_stat_statements` ....

---

## SQL nâng cao senior thực dùng

Đẩy logic vào SQL thường tốt hơn lấy dòng rồi lặp trong Node.

```sql
-- Upsert: insert or update atomically (no read-modify-write race)
INSERT INTO users (email, name) VALUES ($1, $2)
ON CONFLICT (email) DO UPDATE SET name = EXCLUDED.name
RETURNING id;                                   -- RETURNING gives you the row back

-- Window function: rank posts per author WITHOUT collapsing rows
SELECT id, author_id, title,
       row_number() OVER (PARTITION BY author_id ORDER BY created_at DESC) AS rn
FROM posts;

-- CTE for readable multi-step queries (and recursion for trees/graphs)
WITH recent AS (
  SELECT * FROM posts WHERE created_at > now() - interval '7 days'
)
SELECT author_id, count(*) FROM recent GROUP BY author_id;
```

Hai công cụ senior nữa:

- **full-text search** — cho tìm kiếm thật mà không cần engine riêng.
- **`LISTEN`/`NOTIFY`** — pub/sub nhẹ trong Postgres; một write có thể báo cho listener Node real-time.
- **advisory lock** — khóa cấp app cho việc như "chỉ một worker chạy cron này".

> Với phân tích đắt khi tính trực tiếp, **materialized view** cache kết quả và bạn `REFRESH` theo lịch. Với bảng hàng trăm triệu dòng, dùng **partitioning**.

---

## Lời cảnh báo của sư phụ

- **`timestamptz`, luôn luôn.** Lưu giờ local không có zone là lỗi chờ một lần đổi DST.
- **`SELECT *` trong code production là mùi xấu** — nêu tên cột để có index-only scan và app sống sót qua đổi schema.
- **Index khóa ngoại.** Postgres tự index PK nhưng **không** index cột FK — FK không index làm `ON DELETE` và join bò.
- **Migration, không sửa tay.** Mọi đổi schema đi qua migration có version, đảo ngược được.

---

## Thực hành

1. Mô hình blog trong 3NF với khóa, constraint, FK đúng.
2. Viết query chậm, chạy `EXPLAIN ANALYZE`, thêm index đúng, xác nhận `Seq Scan` thành `Index Scan`.
3. Tái hiện lost update với hai client đồng thời, rồi sửa bằng `FOR UPDATE` và lần nữa bằng một `UPDATE` nguyên tử.

---

## Phần tiếp theo

Giờ bạn thiết kế và vận hành được database quan hệ vẫn đúng và nhanh. Viết SQL tay là thiết yếu để *hiểu* — nhưng trong codebase TypeScript lớn bạn muốn type safety và migration mà không mất kiểm soát đó. Ở **Phase 12** ta phủ **Prisma** lên kiến thức Postgres này — một ORM type-safe mà schema, migration, và API query ánh xạ thẳng vào mọi thứ bạn vừa học.
