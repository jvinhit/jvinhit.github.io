---
/**
 * Inline-SVG diagrams for the "Web Security for Frontend Devs" series.
 * Self-hosted, theme-aware (design tokens), one diagram per `kind`.
 * Danger styling (attack / blocked flows) uses the --color-danger token.
 */
type SecurityKind =
  | 'sop'
  | 'xss'
  | 'csp'
  | 'csrf'
  | 'token-storage'
  | 'cors'
  | 'clickjacking'
  | 'headers-tls'
  | 'supply-chain'
  | 'threat-model'
  | 'prototype-pollution'
  | 'dom-clobbering';

interface Props {
  kind: SecurityKind;
  caption?: string;
}

const { kind, caption } = Astro.props;
const a = `sd-arrow-${kind}`;
const ad = `sd-arrow-danger-${kind}`;
---

<figure class="diagram">
  {
    kind === 'sop' && (
      <svg viewBox="0 0 720 230" role="img" aria-label={caption ?? 'Same-Origin Policy'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
        </defs>
        <rect class="d-accent" x="24" y="78" width="200" height="74" rx="6" />
        <text x="124" y="106" text-anchor="middle" class="d-ta">page</text>
        <text x="124" y="126" text-anchor="middle" class="d-ts">https://app.com</text>
        <rect class="d-box" x="470" y="20" width="226" height="60" rx="5" />
        <text x="583" y="46" text-anchor="middle" class="d-t">https://app.com/api</text>
        <text x="583" y="66" text-anchor="middle" class="d-ts">same origin</text>
        <line class="d-stroke" x1="224" y1="104" x2="468" y2="55" marker-end={`url(#${a})`} />
        <text x="360" y="70" text-anchor="middle" class="d-ts">allowed</text>
        <rect class="d-danger-box" x="470" y="148" width="226" height="60" rx="5" />
        <text x="583" y="174" text-anchor="middle" class="d-t">https://evil.com</text>
        <text x="583" y="194" text-anchor="middle" class="d-dt">different origin → read blocked</text>
        <line class="d-dstroke" x1="224" y1="128" x2="468" y2="178" marker-end={`url(#${ad})`} />
        <text x="360" y="170" text-anchor="middle" class="d-dt">blocked</text>
        <text x="360" y="224" text-anchor="middle" class="d-ts">origin = scheme + host + port</text>
      </svg>
    )
  }

  {
    kind === 'xss' && (
      <svg viewBox="0 0 720 220" role="img" aria-label={caption ?? 'XSS attack flow'}>
        <defs>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
        </defs>
        <rect class="d-danger-box" x="20" y="40" width="150" height="56" rx="5" />
        <text x="95" y="64" text-anchor="middle" class="d-dt">Attacker</text>
        <text x="95" y="82" text-anchor="middle" class="d-ts">&lt;script&gt;…&lt;/script&gt;</text>
        <line class="d-dstroke" x1="170" y1="68" x2="244" y2="68" marker-end={`url(#${ad})`} />
        <rect class="d-box" x="246" y="40" width="180" height="56" rx="5" />
        <text x="336" y="64" text-anchor="middle" class="d-t">App stores /</text>
        <text x="336" y="82" text-anchor="middle" class="d-ts">reflects input</text>
        <line class="d-dstroke" x1="426" y1="68" x2="500" y2="68" marker-end={`url(#${ad})`} />
        <rect class="d-box2" x="502" y="40" width="190" height="56" rx="5" />
        <text x="597" y="64" text-anchor="middle" class="d-t">Victim browser</text>
        <text x="597" y="82" text-anchor="middle" class="d-dt">runs script as you</text>
        <line class="d-dstroke" x1="597" y1="96" x2="597" y2="140" marker-end={`url(#${ad})`} />
        <rect class="d-danger-box" x="440" y="142" width="256" height="54" rx="5" />
        <text x="568" y="166" text-anchor="middle" class="d-dt">steal cookies / tokens</text>
        <text x="568" y="184" text-anchor="middle" class="d-ts">keylog · deface · pivot</text>
        <text x="210" y="170" text-anchor="middle" class="d-ts">fix: escape on output,</text>
        <text x="210" y="188" text-anchor="middle" class="d-ts">sanitize HTML, Trusted Types</text>
      </svg>
    )
  }

  {
    kind === 'csp' && (
      <svg viewBox="0 0 720 220" role="img" aria-label={caption ?? 'Content Security Policy'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
        </defs>
        <rect class="d-box" x="20" y="84" width="150" height="56" rx="5" />
        <text x="95" y="110" text-anchor="middle" class="d-t">Browser</text>
        <text x="95" y="128" text-anchor="middle" class="d-ts">about to load</text>
        <line class="d-stroke" x1="170" y1="112" x2="246" y2="112" marker-end={`url(#${a})`} />
        <rect class="d-accent" x="248" y="76" width="190" height="72" rx="6" />
        <text x="343" y="104" text-anchor="middle" class="d-ta">CSP policy</text>
        <text x="343" y="124" text-anchor="middle" class="d-ts">script-src 'self'</text>
        <text x="343" y="140" text-anchor="middle" class="d-ts">'nonce-r4nd'</text>
        <rect class="d-box2" x="520" y="20" width="180" height="50" rx="5" />
        <text x="610" y="50" text-anchor="middle" class="d-t">self / nonce ✓</text>
        <line class="d-stroke" x1="438" y1="100" x2="518" y2="50" marker-end={`url(#${a})`} />
        <text x="486" y="66" text-anchor="middle" class="d-ts">allow</text>
        <rect class="d-danger-box" x="520" y="150" width="180" height="50" rx="5" />
        <text x="610" y="172" text-anchor="middle" class="d-dt">inline / evil.com</text>
        <text x="610" y="190" text-anchor="middle" class="d-ts">blocked + report</text>
        <line class="d-dstroke" x1="438" y1="126" x2="518" y2="172" marker-end={`url(#${ad})`} />
        <text x="486" y="160" text-anchor="middle" class="d-dt">deny</text>
      </svg>
    )
  }

  {
    kind === 'csrf' && (
      <svg viewBox="0 0 720 230" role="img" aria-label={caption ?? 'CSRF attack'}>
        <defs>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
        </defs>
        <rect class="d-box" x="20" y="90" width="160" height="56" rx="5" />
        <text x="100" y="114" text-anchor="middle" class="d-t">Victim browser</text>
        <text x="100" y="132" text-anchor="middle" class="d-ts">logged in @ bank</text>
        <rect class="d-danger-box" x="20" y="20" width="160" height="48" rx="5" />
        <text x="100" y="40" text-anchor="middle" class="d-dt">evil.com</text>
        <text x="100" y="58" text-anchor="middle" class="d-ts">hidden form / img</text>
        <line class="d-dstroke" x1="100" y1="68" x2="100" y2="88" marker-end={`url(#${ad})`} />
        <line class="d-dstroke" x1="180" y1="116" x2="470" y2="116" marker-end={`url(#${ad})`} />
        <text x="325" y="104" text-anchor="middle" class="d-ts">POST /transfer</text>
        <text x="325" y="138" text-anchor="middle" class="d-dt">+ cookie sent automatically</text>
        <rect class="d-box2" x="472" y="90" width="224" height="56" rx="5" />
        <text x="584" y="114" text-anchor="middle" class="d-t">bank.com</text>
        <text x="584" y="132" text-anchor="middle" class="d-ts">trusts the cookie</text>
        <text x="360" y="186" text-anchor="middle" class="d-ts">fix: SameSite cookies + anti-CSRF token</text>
        <text x="360" y="206" text-anchor="middle" class="d-ts">server checks token that evil.com cannot read</text>
      </svg>
    )
  }

  {
    kind === 'token-storage' && (
      <svg viewBox="0 0 720 240" role="img" aria-label={caption ?? 'Token storage: cookie vs localStorage'}>
        <defs>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
        </defs>
        <rect class="d-accent" x="20" y="40" width="320" height="160" rx="8" />
        <text x="40" y="66" class="d-ta">httpOnly cookie</text>
        <rect class="d-box" x="44" y="84" width="272" height="44" rx="5" />
        <text x="180" y="111" text-anchor="middle" class="d-t">Secure · HttpOnly · SameSite</text>
        <text x="40" y="152" class="d-ts">JavaScript cannot read it →</text>
        <text x="40" y="172" class="d-ts">XSS cannot steal the token</text>
        <text x="40" y="190" class="d-ts">needs CSRF defense</text>

        <rect class="d-danger-box" x="380" y="40" width="320" height="160" rx="8" />
        <text x="400" y="66" class="d-dt">localStorage</text>
        <rect class="d-box2" x="404" y="84" width="272" height="44" rx="5" />
        <text x="540" y="111" text-anchor="middle" class="d-t">token = "eyJhbGci…"</text>
        <line class="d-dstroke" x1="540" y1="128" x2="540" y2="150" marker-end={`url(#${ad})`} />
        <text x="540" y="172" text-anchor="middle" class="d-dt">any XSS → localStorage.getItem</text>
        <text x="540" y="190" text-anchor="middle" class="d-ts">→ token exfiltrated</text>
        <text x="360" y="226" text-anchor="middle" class="d-ts">prefer httpOnly cookies for session tokens</text>
      </svg>
    )
  }

  {
    kind === 'cors' && (
      <svg viewBox="0 0 720 240" role="img" aria-label={caption ?? 'CORS preflight'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
        </defs>
        <rect class="d-box" x="20" y="90" width="160" height="60" rx="5" />
        <text x="100" y="116" text-anchor="middle" class="d-t">app.com (JS)</text>
        <text x="100" y="134" text-anchor="middle" class="d-ts">fetch api.other.com</text>
        <rect class="d-box2" x="540" y="90" width="160" height="60" rx="5" />
        <text x="620" y="116" text-anchor="middle" class="d-t">api.other.com</text>
        <text x="620" y="134" text-anchor="middle" class="d-ts">sets ACA-* headers</text>
        <line class="d-stroke" x1="180" y1="40" x2="538" y2="40" marker-end={`url(#${a})`} />
        <text x="360" y="30" text-anchor="middle" class="d-ts">1 · OPTIONS preflight (Origin, method, headers)</text>
        <line class="d-stroke" x1="538" y1="74" x2="182" y2="74" marker-end={`url(#${a})`} />
        <text x="360" y="64" text-anchor="middle" class="d-ta">2 · Access-Control-Allow-Origin: app.com</text>
        <line class="d-stroke" x1="180" y1="180" x2="538" y2="180" marker-end={`url(#${a})`} />
        <text x="360" y="170" text-anchor="middle" class="d-ts">3 · real request (only if allowed)</text>
        <text x="360" y="220" text-anchor="middle" class="d-ts">CORS relaxes SOP — the SERVER decides who may read the response</text>
      </svg>
    )
  }

  {
    kind === 'clickjacking' && (
      <svg viewBox="0 0 720 220" role="img" aria-label={caption ?? 'Clickjacking overlay'}>
        <rect class="d-danger-box" x="60" y="30" width="380" height="150" rx="8" />
        <text x="80" y="56" class="d-dt">evil.com — decoy page</text>
        <rect class="d-box" x="120" y="86" width="220" height="54" rx="6" />
        <text x="230" y="118" text-anchor="middle" class="d-t">"Win a prize 🎁"</text>
        <rect class="d-accent" x="150" y="92" width="160" height="42" rx="6" opacity="0.55" />
        <text x="230" y="190" text-anchor="middle" class="d-ts">invisible bank.com iframe on top</text>
        <text x="470" y="70" class="d-ts">victim thinks they</text>
        <text x="470" y="90" class="d-ts">click the button…</text>
        <text x="470" y="116" class="d-dt">…but click lands on</text>
        <text x="470" y="136" class="d-dt">"Transfer" in the iframe</text>
        <text x="470" y="172" class="d-ts">fix: frame-ancestors 'none'</text>
        <text x="470" y="192" class="d-ts">/ X-Frame-Options: DENY</text>
      </svg>
    )
  }

  {
    kind === 'headers-tls' && (
      <svg viewBox="0 0 720 230" role="img" aria-label={caption ?? 'HTTPS and security headers'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
        </defs>
        <rect class="d-box" x="20" y="84" width="150" height="56" rx="5" />
        <text x="95" y="116" text-anchor="middle" class="d-t">Browser</text>
        <line class="d-stroke" x1="170" y1="112" x2="244" y2="112" marker-end={`url(#${a})`} />
        <text x="207" y="100" text-anchor="middle" class="d-ta">TLS 🔒</text>
        <rect class="d-accent" x="246" y="40" width="290" height="150" rx="8" />
        <text x="266" y="66" class="d-ta">response headers</text>
        {[
          'Strict-Transport-Security',
          'Content-Security-Policy',
          'X-Content-Type-Options',
          'Referrer-Policy',
          'X-Frame-Options',
        ].map((t, i) => (
          <text x="266" y={92 + i * 20} class="d-ts">{`· ${t}`}</text>
        ))}
        <line class="d-stroke" x1="536" y1="112" x2="610" y2="112" marker-end={`url(#${a})`} />
        <rect class="d-box2" x="612" y="84" width="88" height="56" rx="5" />
        <text x="656" y="116" text-anchor="middle" class="d-t">Server</text>
        <text x="360" y="216" text-anchor="middle" class="d-ts">HTTPS everywhere + headers set the browser's security defaults</text>
      </svg>
    )
  }

  {
    kind === 'supply-chain' && (
      <svg viewBox="0 0 720 230" role="img" aria-label={caption ?? 'Supply-chain risk'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
        </defs>
        <rect class="d-accent" x="20" y="86" width="150" height="56" rx="5" />
        <text x="95" y="112" text-anchor="middle" class="d-ta">Your app</text>
        <text x="95" y="130" text-anchor="middle" class="d-ts">package.json</text>
        <line class="d-stroke" x1="170" y1="114" x2="244" y2="114" marker-end={`url(#${a})`} />
        <rect class="d-box" x="246" y="86" width="170" height="56" rx="5" />
        <text x="331" y="112" text-anchor="middle" class="d-t">direct deps</text>
        <text x="331" y="130" text-anchor="middle" class="d-ts">12 packages</text>
        <line class="d-stroke" x1="416" y1="114" x2="490" y2="114" marker-end={`url(#${a})`} />
        <rect class="d-box2" x="492" y="40" width="208" height="50" rx="5" />
        <text x="596" y="70" text-anchor="middle" class="d-t">transitive deps × 900</text>
        <line class="d-stroke" x1="416" y1="100" x2="490" y2="70" marker-end={`url(#${a})`} />
        <rect class="d-danger-box" x="492" y="140" width="208" height="56" rx="5" />
        <text x="596" y="164" text-anchor="middle" class="d-dt">1 malicious / typosquat</text>
        <text x="596" y="182" text-anchor="middle" class="d-ts">runs in YOUR build & page</text>
        <line class="d-dstroke" x1="416" y1="128" x2="490" y2="168" marker-end={`url(#${ad})`} />
        <text x="300" y="220" text-anchor="middle" class="d-ts">lockfiles · npm audit · pin versions · SRI for CDNs</text>
      </svg>
    )
  }

  {
    kind === 'threat-model' && (
      <svg viewBox="0 0 720 230" role="img" aria-label={caption ?? 'Trust boundary'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
        </defs>
        <rect class="d-danger-box" x="20" y="30" width="300" height="170" rx="8" />
        <text x="40" y="56" class="d-dt">CLIENT — untrusted</text>
        <text x="40" y="84" class="d-ts">· user can edit JS, DOM,</text>
        <text x="40" y="104" class="d-ts">  requests, localStorage</text>
        <text x="40" y="124" class="d-ts">· validation here = UX only</text>
        <text x="40" y="152" class="d-ts">· no secrets, no real authz</text>
        <line class="d-stroke" x1="320" y1="115" x2="398" y2="115" marker-end={`url(#${a})`} />
        <text x="360" y="104" text-anchor="middle" class="d-ta">trust boundary</text>
        <text x="360" y="142" text-anchor="middle" class="d-ts">validate + authz here</text>
        <rect class="d-accent" x="400" y="30" width="300" height="170" rx="8" />
        <text x="420" y="56" class="d-ta">SERVER — trusted</text>
        <text x="420" y="84" class="d-ts">· re-validate every input</text>
        <text x="420" y="104" class="d-ts">· enforce authn / authz</text>
        <text x="420" y="124" class="d-ts">· hold secrets & business rules</text>
        <text x="420" y="152" class="d-ts">· never trust the client</text>
        <text x="360" y="220" text-anchor="middle" class="d-ts">every input crossing the boundary is hostile until proven safe</text>
      </svg>
    )
  }

  {
    kind === 'prototype-pollution' && (
      <svg viewBox="0 0 720 250" role="img" aria-label={caption ?? 'Prototype pollution propagates to every object'}>
        <defs>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
        </defs>
        <rect class="d-danger-box" x="20" y="24" width="232" height="58" rx="5" />
        <text x="136" y="48" text-anchor="middle" class="d-dt">attacker JSON</text>
        <text x="136" y="68" text-anchor="middle" class="d-ts">{'{"__proto__":{"isAdmin":true}}'}</text>
        <line class="d-dstroke" x1="252" y1="53" x2="318" y2="53" marker-end={`url(#${ad})`} />
        <rect class="d-box" x="320" y="24" width="150" height="58" rx="5" />
        <text x="395" y="48" text-anchor="middle" class="d-t">deepMerge()</text>
        <text x="395" y="68" text-anchor="middle" class="d-ts">walks __proto__</text>
        <line class="d-dstroke" x1="470" y1="53" x2="556" y2="53" marker-end={`url(#${ad})`} />
        <rect class="d-accent" x="470" y="96" width="230" height="46" rx="6" />
        <text x="585" y="124" text-anchor="middle" class="d-ta">Object.prototype (shared)</text>
        <line class="d-dstroke" x1="558" y1="60" x2="585" y2="94" marker-end={`url(#${ad})`} />
        {['{} user', '{} cart', '{} config'].map((t, i) => (
          <g>
            <rect class="d-box2" x={40 + i * 220} y="170" width="180" height="48" rx="5" />
            <text x={130 + i * 220} y="190" text-anchor="middle" class="d-t">{t}</text>
            <text x={130 + i * 220} y="208" text-anchor="middle" class="d-dt">.isAdmin → true</text>
            <line class="d-dstroke" x1={585 - 0} y1="142" x2={130 + i * 220} y2="168" marker-end={`url(#${ad})`} />
          </g>
        ))}
        <text x="360" y="242" text-anchor="middle" class="d-ts">one polluted prototype is inherited by every plain object on the page</text>
      </svg>
    )
  }

  {
    kind === 'dom-clobbering' && (
      <svg viewBox="0 0 720 240" role="img" aria-label={caption ?? 'DOM clobbering overrides a global lookup'}>
        <defs>
          <marker id={ad} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead-danger" />
          </marker>
          <marker id={a} markerWidth="9" markerHeight="9" refX="6.5" refY="3" orient="auto">
            <path d="M0,0 L7,3 L0,6 Z" class="d-arrowhead" />
          </marker>
        </defs>
        <rect class="d-danger-box" x="20" y="26" width="300" height="64" rx="5" />
        <text x="40" y="50" class="d-dt">injected HTML — no &lt;script&gt;</text>
        <text x="40" y="72" class="d-ts">&lt;a id="CONFIG" name="url" href="//evil"&gt;</text>
        <line class="d-dstroke" x1="170" y1="90" x2="170" y2="120" marker-end={`url(#${ad})`} />
        <rect class="d-box" x="20" y="122" width="300" height="56" rx="5" />
        <text x="170" y="146" text-anchor="middle" class="d-t">window.CONFIG</text>
        <text x="170" y="166" text-anchor="middle" class="d-dt">now points at the &lt;a&gt; element</text>
        <rect class="d-box2" x="392" y="40" width="308" height="64" rx="5" />
        <text x="412" y="64" class="d-ts">if (window.CONFIG) {'{'}</text>
        <text x="412" y="86" class="d-dt">  loadScript(CONFIG.url) // //evil</text>
        <line class="d-stroke" x1="320" y1="150" x2="390" y2="84" marker-end={`url(#${a})`} />
        <rect class="d-accent" x="392" y="128" width="308" height="62" rx="6" />
        <text x="412" y="152" class="d-ta">fix</text>
        <text x="412" y="172" class="d-ts">getElementById + instanceof check; sanitize id/name</text>
        <text x="360" y="226" text-anchor="middle" class="d-ts">CSP that blocks scripts does NOT stop DOM clobbering — it is pure HTML</text>
      </svg>
    )
  }

  {caption && <figcaption>{caption}</figcaption>}
</figure>

<style>
  .diagram {
    margin: 1.5rem 0;
    border: 1px solid var(--color-border);
    border-radius: 8px;
    background: var(--color-bg);
    padding: 0.85rem 0.9rem 0.6rem;
  }
  .diagram svg {
    display: block;
    width: 100%;
    height: auto;
  }
  .diagram figcaption {
    margin-top: 0.5rem;
    font-size: 0.72rem;
    color: var(--color-fg-muted);
    text-align: center;
    letter-spacing: 0.02em;
  }
  .diagram :global(text) {
    font-family: var(--font-mono);
  }
  .diagram :global(.d-box) {
    fill: var(--color-surface);
    stroke: var(--color-border);
    stroke-width: 1;
  }
  .diagram :global(.d-box2) {
    fill: var(--color-surface-2);
    stroke: var(--color-border);
    stroke-width: 1;
  }
  .diagram :global(.d-accent) {
    fill: color-mix(in srgb, var(--color-accent) 12%, transparent);
    stroke: var(--color-accent);
    stroke-width: 1;
  }
  .diagram :global(.d-danger-box) {
    fill: color-mix(in srgb, var(--color-danger) 12%, transparent);
    stroke: var(--color-danger);
    stroke-width: 1;
  }
  .diagram :global(.d-stroke) {
    fill: none;
    stroke: var(--color-border-strong);
    stroke-width: 1.4;
  }
  .diagram :global(.d-dstroke) {
    fill: none;
    stroke: var(--color-danger);
    stroke-width: 1.4;
    stroke-dasharray: 5 4;
  }
  .diagram :global(.d-arrowhead) {
    fill: var(--color-border-strong);
  }
  .diagram :global(.d-arrowhead-danger) {
    fill: var(--color-danger);
  }
  .diagram :global(.d-t) {
    fill: var(--color-fg);
    font-size: 13px;
  }
  .diagram :global(.d-tm) {
    fill: var(--color-fg-muted);
    font-size: 12px;
  }
  .diagram :global(.d-ts) {
    fill: var(--color-fg-subtle);
    font-size: 11px;
  }
  .diagram :global(.d-ta) {
    fill: var(--color-accent);
    font-size: 12.5px;
  }
  .diagram :global(.d-dt) {
    fill: var(--color-danger);
    font-size: 12.5px;
  }
  .diagram :global(.d-title) {
    fill: var(--color-fg-muted);
    font-size: 12px;
    letter-spacing: 0.1em;
  }
</style>
